import { motion } from "framer-motion";
import { useState } from "react";

export default function WeddingInvitation() {
  const [slide, setSlide] = useState(0);
  const slides = [
    "cover", "ayat", "details", "location", "rsvp", "closing"
  ];

  const nextSlide = () => setSlide((slide + 1) % slides.length);
  const prevSlide = () => setSlide((slide - 1 + slides.length) % slides.length);

  return (
    <div className="w-full h-screen flex flex-col items-center justify-center bg-white text-gray-800 overflow-hidden relative">
      {/* Background gradient gold */}
      <div className="absolute inset-0 bg-gradient-to-b from-white via-amber-50 to-amber-100 opacity-70"></div>

      {/* Smooth slide transition */}
      <motion.div
        key={slides[slide]}
        initial={{ opacity: 0, y: 40 }}
        animate={{ opacity: 1, y: 0 }}
        exit={{ opacity: 0, y: -40 }}
        transition={{ duration: 0.8 }}
        className="text-center p-6 z-10 max-w-xl"
      >
        {slides[slide] === "cover" && (
          <div>
            <h1 className="text-4xl font-serif text-amber-700 mb-2">Undangan Pernikahan</h1>
            <h2 className="text-5xl font-semibold text-gray-900">Yanto & Dimas</h2>
            <p className="mt-6 italic text-gray-600">
              Minggu, 7 Februari 2027<br />
              Pondok Bambu Kalibaru, Banyuwangi
            </p>
          </div>
        )}

        {slides[slide] === "ayat" && (
          <div>
            <p className="italic text-gray-700 mb-4">
              “Dan di antara tanda-tanda (kebesaran)-Nya ialah Dia menciptakan
              untukmu pasangan-pasangan dari jenismu sendiri, supaya kamu
              cenderung dan merasa tenteram kepadanya, dan dijadikan-Nya di
              antaramu rasa kasih dan sayang.”
            </p>
            <p className="font-semibold text-amber-700">(QS. Ar-Rum: 21)</p>
          </div>
        )}

        {slides[slide] === "details" && (
          <div>
            <h3 className="text-2xl font-semibold mb-2 text-amber-700">Acara Pernikahan</h3>
            <p>Minggu, 7 Februari 2027<br />Pukul 15:00 WIB – selesai</p>
            <p className="mt-3 font-semibold">Tempat:</p>
            <p>Pondok Bambu Kalibaru, Banyuwangi, Jawa Timur</p>
          </div>
        )}

        {slides[slide] === "location" && (
          <div>
            <h3 className="text-2xl font-semibold mb-2 text-amber-700">Lokasi Acara</h3>
            <iframe
              title="Lokasi Pondok Bambu"
              src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3944.651707002159!2d114.150!3d-8.266!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x2dd166b3c50a8c33%3A0x6a3f73d3edc5dc14!2sKalibaru%2C%20Banyuwangi!5e0!3m2!1sen!2sid!4v0000000000"
              width="100%"
              height="250"
              className="rounded-2xl shadow-md border-2 border-amber-200"
              allowFullScreen
            ></iframe>
          </div>
        )}

        {slides[slide] === "rsvp" && (
          <div>
            <h3 className="text-2xl font-semibold text-amber-700 mb-3">Konfirmasi Kehadiran</h3>
            <form
              action="https://formsubmit.co/youremail@example.com"
              method="POST"
              className="space-y-3"
            >
              <input
                type="text"
                name="Nama"
                placeholder="Nama Anda"
                className="w-full border rounded-xl p-2"
                required
              />
              <select
                name="Kehadiran"
                className="w-full border rounded-xl p-2"
                required
              >
                <option value="">Konfirmasi Kehadiran</option>
                <option value="Hadir">Hadir</option>
                <option value="Tidak Hadir">Tidak Hadir</option>
              </select>
              <button
                type="submit"
                className="bg-amber-600 hover:bg-amber-700 text-white px-6 py-2 rounded-xl shadow"
              >
                Kirim
              </button>
            </form>
          </div>
        )}

        {slides[slide] === "closing" && (
          <div>
            <p className="text-gray-700 italic mb-2">
              Kehadiran dan doa restu Anda merupakan kebahagiaan bagi kami.
            </p>
            <h3 className="text-3xl font-semibold text-amber-700">Yanto & Dimas</h3>
          </div>
        )}
      </motion.div>

      {/* Navigation */}
      <div className="absolute bottom-10 flex gap-6 z-20">
        <button
          onClick={prevSlide}
          className="text-amber-600 hover:text-amber-800 font-semibold"
        >
          ‹ Sebelumnya
        </button>
        <button
          onClick={nextSlide}
          className="text-amber-600 hover:text-amber-800 font-semibold"
        >
          Selanjutnya ›
        </button>
      </div>
    </div>
  );
}
